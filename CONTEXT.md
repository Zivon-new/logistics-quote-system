# CONTEXT.md — 项目领域词汇表

本文件是项目的语言基准。所有对话、代码审查、需求讨论均以此为准。

---

## 核心术语

### 路线（Route）
一次跨境货运委托的物理描述。由起始地、途径地（可选）、目的地、交易日期区间、实际重量、总体积、货值构成。一条路线可以对应多个代理方案。

对应数据库表：`routes`

---

### 代理商
提供货运服务的货代公司实体，以公司名称标识。

对应字段：`route_agents.代理商`

---

### 代理方案（RouteAgent）
某家代理商针对某条路线给出的完整报价。包含运输方式、贸易类型、时效、不含项、赔付条款，以及该方案下的所有费用明细和整单费用。

一条路线下可有多个代理方案（来自不同代理商或同一代理商的不同方案）。

对应数据库表：`route_agents`

---

### 计费重量
用于计算运费的重量基准（单位：kg）。当前由人工录入或从 Excel 原表读取，系统暂不自动计算。

> 行业背景：通常取实际重量与体积重量（总体积 × 换算系数）的较大值，但本系统尚未实现该公式。

对应字段：`routes.计费重量`

---

### 费用明细（FeeItem）
代理方案下按"单价 × 数量"计算的单项费用行。含费用类型、单价、单位、数量、最低收费、币种。

对应数据库表：`fee_items`

---

### 整单费用（FeeTotal）
代理方案下不拆分计量、以总额直接呈现的费用项。不含单价/数量，只有费用名称和金额。常用于目的港杂费等一口价收费。

对应数据库表：`fee_total`

---

### 税率明细（TaxDetail）
代理方案在「多货物税率」模式下，按货物逐行列出的税率配置。每行含：货物名称、货值、货值币种、HS编码、原产地、税率说明、综合税率%。税金由系统自动汇总计算。

同一路线下多个代理方案的税率明细通常相同（货物不变），可通过「从其他代理同步」功能一键复制。

对应字段：`summary.税率明细`（JSON，存于 summary 表）
